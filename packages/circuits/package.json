{
  "name": "@zkllmapi/circuits",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "nargo compile 2>&1 && bb generate-verification-key --package ./target/circuits.json --output ./target/vk && echo 'VK generated'",
    "compile": "nargo compile 2>&1",
    "typecheck": "echo 'Nargo handles its own types'"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
